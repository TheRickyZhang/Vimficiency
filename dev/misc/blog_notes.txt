You need to be very clear with LLMs!

One thing that is very helpful is having it create randomized tests with an objective truth verifier, such as: generating a random buffer / edit location, and verifying that the result is the same as running it directly through a Neovim instance.

On the flip side, it can be a real pain if your tests have a subtle flaw, as it is generally assumed they are ironclad. For instance, I did not set up my EditBoundary to have private members, so rather than using constructor directly, Claude Code created a proxy instance, then changed its member variables directly, which was incorrect compared to the actual situation. Don't rely on Claude making correct tests for you without some backing source!

There was massive performance difference between pure deletions (fast) and regular edits (slow), which didn't make sense as they were performing same core work - after investigation we found that there was a missing break condition in edits that caused massive reexploration, causing massive (>100x) speedups:
┌─────────┬────────────────────┬─────────────────────────────┐
  │  Case   │    Before (A*)     │ After (A* + early stopping) │
  ├─────────┼────────────────────┼─────────────────────────────┤
  │ 2L→1w   │ 1021ms, 9/30 found │ 4ms, 10 found               │
  ├─────────┼────────────────────┼─────────────────────────────┤
  │ 3L→1w   │ 5341ms, 1 found    │ 17ms, 19 found              │
  ├─────────┼────────────────────┼─────────────────────────────┤
  │ 2L rand │ 918ms, 3 found     │ 1.6ms, 9 found              │

Little things can have huge impacts on code.
  ┌───────────────────────┬───────────┬───────────┬─────────┐
  │       Benchmark       │ Regressed │ After fix │ Speedup │
  ├───────────────────────┼───────────┼───────────┼─────────┤
  │ SmallChange/2         │ 74.7 ms   │ 0.33 ms   │ 227x    │
  ├───────────────────────┼───────────┼───────────┼─────────┤
  │ SmallChange/3         │ 110.5 ms  │ 0.55 ms   │ 201x    │
  ├───────────────────────┼───────────┼───────────┼─────────┤
  │ SmallEmbeddedChange/1 │ 57.6 ms   │ 1.1 ms    │ 52x     │
  ├───────────────────────┼───────────┼───────────┼─────────┤
  │ SmallEmbeddedChange/2 │ 93.6 ms   │ 3.4 ms    │ 28x     │
  ├───────────────────────┼───────────┼───────────┼─────────┤
  │ SmallEmbeddedChange/3 │ 106.9 ms  │ 7.2 ms    │ 15x     │
  └───────────────────────┴───────────┴───────────┴─────────┘
  Performance fully restored. The issue was that moving replayAndCacheSuffix from find-time to pop-time starved the suffix
  cache — intermediate states never got cached, so every starting position redundantly explored the full search space.
  Restoring find-time calls (with the complete goal suffix in cached entries) brings back the cache's pruning power.


How to solve problem of stale benchmark data history when there are force pushes / retrospective merges that overwrite existing, requiring commit <-> data sync.



Some examples of stupid code:

// Resolve BufferIndex: use external if provided, otherwise build local
BufferIndex localBufferIndex(lines);
if (!bufferRef) {
  bufferRef = {&localBufferIndex, 0};:w

}
creates a buffer index every time at first even though the goal was to use a precomputed buffer index if known!



❯   // Convert to range params
    NavOptimizeRangeParams rangeParams;
    static_cast<NavOptimizeParams&>(rangeParams) = params;
    rangeParams.allowMultiplePerPosition = true;
  \
  \
This code is so fucking stupid. First of all, the point of different types is to not be able to cats them like this. Second, now you are completely ruining he the semantics by making it range-style when it is clearly not!!


Write article about snake_case vs PascalCase for namespaces.

Also conventions like this:\
https://accu.org/journals/overload/22/121/wakely_1923/
