int main() {
  int x = 0;
  for(int i = 0; i < 10; i++) {
    x += i;
  }
}

int main() {
  int x = 0;
  int n = 10;
  for(int i = 0; i < n; i++) {
    x -= foo(i);
  }
}


== Current priority stack:

== TODO Stack

- apply the same single-token design fix to compositionFrontier, which can emit combined movement + edit motions like $a{typed}<Esc>

- Add different types of tests: expect, property-based, fuzzing
- Improve our diff separation logic using tree DP diff idea by Tristan Hume
- Fix the visual nav branch in TransformOptimizer; moving in visual mode can be DIFFERENT than in normal mode
- Add some more randomized exploration in optimizers so it can explore more branches (probably want a deterministics ration). Actually, probably modifying our approach to use Anytime Repairing A* is better - find the best approach we can in a specific time frame, given that a decently admissible heuristic is not possible.
- Mostly work on getting MVP for now




- Later:
- Given that we have a visual-nav case in our transform optimizer, should the transform params contain a nav param for this case? If we proceed like this we suddenly need an embedded composition model
- Any need for concurrency in implementing subprocesses?
- Logic for consolidating insert mode text into a shorter reference / symbol.
- Think about good .agents skills to add along the way, and test to see if they work well.
- Make structs like CursorPos, CharRange, immutable?
- Expanding search so that we include 1 before / after so that a/i possibly searches as well;
- Would need to update both the optmizier AND the explorer identically
- Probably would want to fully figure out how insert-mode deletions with <C-w>, <C-u> would work.
- Review implementation of BufferIndex in optimizeToRange() (is it exploring correctly on lines / columns? Correct passed in from composition?)
- 9dedexC explored when C would suffice?
- Improving the lua/C++ interaction semantics
- Are param handling consistent?
- Right now our parsing handles find state like: faw;, where the second ; works from the current position even after a w in between.
- Really ensure that the suffix cache handling with . is correct AND performant.
Local-only RunningEffort context in NavOptimize
- Improve onLinewiseGoal semantics - currently very lengthy
- Wait until https://github.com/google/fuzztest/pull/2082 gets merged to fix annoying watchdog issue


Review Exploration C++ code
toNavSuggestion - huh wtf are these conversions?
== Later:
Optimize our VimCore word motions to use stored flags to drive decisions, just like in our initial DP diff processing

Write a vim benchmark test as a way to try to measure progress, with parts: direct instruction (go to highlighted, and delete), start/end buffer (change A -> B), general tasks (after reading about an algorithm, modify current impl to be correct, or refactor some code in a specified way).
Migrate to modules when greater scope
Always keep hashing for speedup in mind

Can skip F-motions if no endpoints found?

constexpr disabling of searched motions in MotionExplorer/EditExplorer

== Things missed by optimizer ()

== Misc:
- Distinguish between USER_EXPLORE_FACTOR and ABSOLUTE_EXPLORE_FACTOR (stop at minimum of the results with these) 
Insert

Ranked by natural development order and priority. Complexity level (?) in * stars.
- simple symbols: *, #, %                          ***
- Niche commands: -/+, ~, gu, gU,                  *
- Search, n/N (heuristic best-matching prefix)     ***
- Separate pattern detection for potential macros? ***
- Visual Mode                                      ******
- Multiple buffers with gd, gr, link jumps         ******
- Feedback on preferred motion paradigms over time ****
- minimal register/macro use detecting repetition  ***
- Reinforcement learning?                          *****

Done:
- Simulation                                       **
- Build word indices                               **
- Numbers                                          ***
- f/t, ;/, motions                                 **
- <C-d, u, f, b> page motions                      **
- Real-time, automatic session spawning            *****
- Insert Mode handling (just a lot more stuff)     ********* (very very difficult)

