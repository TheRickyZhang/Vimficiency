#pragma once

// Generated from src/BuildConfig.h.in by CMake — do not edit this file directly.
// Add new build-time flags in CMakeLists.txt and this template together, and
// have source code read them with `#if`, not `#ifdef`. `#cmakedefine01` always
// emits the macro as either 0 or 1, so a typo like `#if VIMF_TRAK_STATES`
// evaluates to 0 under warnings-as-errors tooling rather than silently taking
// the "disabled" branch.

#cmakedefine01 VIMF_TRACK_STATES
